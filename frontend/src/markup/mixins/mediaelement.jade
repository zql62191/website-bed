mixin media-element-video(klass, pathtofile, fallback, w, h)
    - fallback = (typeof fallback !== 'undefined' && (fallback == true || fallback == false)) ? fallback : false;
    - pathtofile = (typeof pathtofile !== 'undefined') ? pathtofile : '';


    //- TODO: file type checking (allow only mp4, ogg, webm)

    if fallback
        video(width='#{w}', height='#{h}', poster='poster.jpg', controls='controls', preload='none' class=klass)
          //- MP4 for Safari, IE9, iPhone, iPad, Android, and Windows Phone 7
          source(type='video/mp4', src='#{pathtofile}')
          //- WebM/VP8 for Firefox4, Opera, and Chrome
          source(type='video/webm', src='myvideo.webm')
          //- Ogg/Vorbis for older Firefox and Opera versions
          source(type='video/ogg', src='myvideo.ogv')
          //- Optional: Add subtitles for each language
          track(kind='subtitles', src='subtitles.srt', srclang='en')
          //- Optional: Add chapters
          track(kind='chapters', src='chapters.srt', srclang='en')
          //- Flash fallback for non-HTML5 browsers without JavaScript
          object(width='#{w}', height='#{h}', type='application/x-shockwave-flash', data='me/flashmediaelement.swf')
            param(name='movie', value='me/flashmediaelement.swf')
            param(name='flashvars', value='controls=true&file=#{pathtofile}')
            //- Image as a last resort
            img(src='#{pathtofile}', width='#{w}', height='#{h}', title='No video playback capabilities')
    else
        video(width='#{w}' height='#{h}' src='#{pathtofile}')


mixin media-element-audio(klass, pathtofile, fallback)
    - fallback = (typeof fallback !== 'undefined' && (fallback == true || fallback == false)) ? fallback : false;
    - pathtofile = (typeof pathtofile !== 'undefined') ? pathtofile : '';

    //- TODO: file type checking (allow only mp3 [?])

    if fallback
        //- Is there even a fallback option for audio?
    else
        audio(width="220",src='#{pathtofile}' class=klass)
